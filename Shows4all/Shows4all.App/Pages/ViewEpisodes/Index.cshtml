@page
@model Shows4all.App.Pages.ViewEpisodes.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Episodes</h1>


<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Episode[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Episode[0].EpisodeNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Episode[0].Season.IdSerie)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Episode) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EpisodeNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Season.IdSerie)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Season.IdSerie)
            </td>
        </tr>
}
    </tbody>
</table>
