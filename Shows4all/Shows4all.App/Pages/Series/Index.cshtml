@page
@model Shows4all.App.Pages.Series.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Serie Management Menu</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Serie[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Serie[0].Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Serie[0].Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Serie[0].ReleaseDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Serie[0].Genre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Serie[0].Country)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Serie)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Price)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ReleaseDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Genre.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Country.Name)
                </td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<h2>Manage Seasons</h2>

<p><a href="/seasons/index">Seasons</a></p>

<h2>Manage Episodes</h2>

<p><a href="/episodes">Episodes</a></p>

<h2>Manage Genre</h2>

<p><a href="/genres">Genre</a></p>

<h2>Manage Country</h2>

<p><a href="/countries">Country</a></p>


<div>
    <p><a href="/user">Back to Menu</a>
</div>
