@page
@model Shows4all.App.Pages.Payments.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Payments</h1>

<p>
    <a asp-page="Create">New Payment</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Payment[0].Rental)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Payment[0].CrediCardPayment)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Payment) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Rental.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CrediCardPayment.CardName)
            </td>
            <td>
                @*<a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |*@
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> 
                @*<a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>*@
            </td>
        </tr>
}
    </tbody>
</table>
