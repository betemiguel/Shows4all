@page
@model Shows4all.App.Pages.Payments.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Payment</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Payment.Rental)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Payment.Rental.Id)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Payment.CrediCardPayment)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Payment.CrediCardPayment.Id)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Payment.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
